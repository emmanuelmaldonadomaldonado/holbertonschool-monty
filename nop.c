#include "monty.h"

/**
 * nop - Function  for opcode nop that does nothing.
 *
 * @stack: Pointer to the top of the stack.
 * @line_number: Line number in the monty file.
 */

void nop(stack_t **stack, unsigned int line_number)
{
    (void)stack;
    (void)line_number;

    /* Does nothing */
}
